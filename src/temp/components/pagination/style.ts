import { createStyles } from '@material-ui/styles';

const styles = () =>
  createStyles({
    paginationContainer: {
      display: 'flex',
      justifyContent: 'end',
      alignItems: 'center',
      gap: '.5rem'
    }
  });

export default styles;
